# Copy to sagemaker/secrets.env (gitignored) and fill in.
# Every KEY=value here is injected into the training container environment.
WANDB_API_KEY=
# Optional - Qwen3-VL is public so usually unneeded; set for gated repos/rate limits:
# HF_TOKEN=
